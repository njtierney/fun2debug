.Rproj.user
.Rhistory
.RData
.Ruserdata
dev
docs
/.quarto/
*.aux
*.log
*.pdf
*.toc
# `keep-tex: true` writes this one file, so name it rather than the glob.
Introduction-to-Functions-and-Debugging.tex
# Rendering leaves copies of these next to the sources as well as in docs/.
site_libs
*_files

**/*.quarto_ipynb
